{% extends 'base.html.twig' %}

{% block body %}
   <div class="well">
       <h2>Settings {{ app.user.fullname|default(app.user.username) }} </h2>
       <div class="row">
           <div class="col-md-3">
               {{ form_start(form) }}
               {{ form_widget(form) }}
               <input class="btn btn-primary" type="submit" value="Save" />
               {{ form_end(form) }}
           </div>
       </div>
   </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}
